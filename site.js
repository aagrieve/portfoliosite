const aboutPage = () => {};

const workPage = () => {};

const projectPage = () => {};

const contactPage = () => {};

// add onclick listeners to each tab in navbar
// automatically start on about page
